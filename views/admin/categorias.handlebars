

<h2>Lista de Categorias:</h2>
<hr>
<a href="/admin/categorias/add"><button class="btn btn-success"  style="background-color: #ce3f67;">Nova Categoria</button></a>

{{#each categorias}}
  <div class="card mt-4"> 
  <div class="card-body">
      <h4>
          {{nome}}
      </h4>
      <small>Slug: {{slug}}</small><br>
      <small>Data de Postagem: {{date}}</small><br>
      <a href="/admin/categorias/edit/{{_id}}"><button class="btn btn-success mt-2"  style="background-color: #b9788a;" >Editar Categoria</button></a>
      <form action="/admin/categorias/deletar" method="POST">
        <input type="hidden" name="id" value="{{_id}}">
        <button type="submit" class="btn btn-success mt-4" style="background-color: #70414f;">Deletar</button>
      </form>
    </div>
  </div>
{{else}}
   <h4 class="mt-3">Nenhuma categoria registrada!</h4>
{{/each}}